const items = document.querySelectorAll('.reveal'); if ('IntersectionObserver' in window) { const io = new IntersectionObserver((entries) => { entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add('in'); io.unobserve(entry.target); } }); }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' }); items.forEach((el, i) => { el.style.transitionDelay = `${Math.min((i % 4) * 55, 165)}ms`; io.observe(el); }); } else { items.forEach(el => el.classList.add('in')); } const copyPilot = document.getElementById('copyPilot'); if (copyPilot) { copyPilot.addEventListener('click', async () => { const message = "pilot"; try { await navigator.clipboard.writeText(message); copyPilot.textContent = 'Copied — reply to the text'; } catch { copyPilot.textContent = 'Reply “pilot” to the text'; } }); }